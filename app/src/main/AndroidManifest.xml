<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <permission
        android:name="com.example.appsicenet.permission.READ_SICENET"
        android:protectionLevel="normal" />

    <permission
        android:name="com.example.appsicenet.permission.WRITE_SICENET"
        android:protectionLevel="normal" />

    <application
        android:name=".AppSicenet"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppSicenet">
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="remove" />

        <provider
            android:name=".provider.SicenetProvider"
            android:authorities="com.example.sicenet.provider"
            android:exported="true"
            android:readPermission="com.example.appsicenet.permission.READ_SICENET"
            android:writePermission="com.example.appsicenet.permission.WRITE"
            />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppSicenet">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>